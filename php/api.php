<?php
if (!empty($_POST['data'])) {
  $data = $_POST['data'];
  var_dump($_POST);
  // $fname = ; //generates random name

  // $file = fopen("upload/" . $fname, 'w'); //creates new file
  // fwrite($file, $data);
  // fclose($file);
}
?>